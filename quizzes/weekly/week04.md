# Weekly Quiz — Week 4 (Injection)

**~10 min · in-class · 6 questions · low-stakes** (drop lowest). No devices / locked browser.

**Name:** ____________  **Student ID:** ________

## MCQ (5 × 1)
1. The input `' OR '1'='1` against a login form targets:
   a) XSS  b) SQL injection (auth bypass)  c) path traversal  d) CSRF
2. The strongest defense against SQL injection is:
   a) escaping quotes by hand  b) a WAF only  c) parameterized queries / prepared statements  d) hiding error messages
3. OS command injection happens when:
   a) a file is too large  b) user input is passed into a shell command  c) the DB is offline  d) cookies are missing
4. The CWE id for SQL injection is:
   a) CWE-79  b) CWE-22  c) CWE-89  d) CWE-798
5. Which does **not** by itself stop SQLi in the query?
   a) prepared statements  b) parameter binding  c) an ORM with bound params  d) HTML output encoding

## Short answer — 🔒 your own work (1 × 3)
6. From this week's **SQLi Boss Fight**, paste the **exact payload** you used to log in as admin and explain in one sentence **why** it worked.
