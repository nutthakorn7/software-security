# Sandbox/teaching only; for authorized lab use.
#
# Week 12 — Supply-Chain / SCA practice.
# These versions are INTENTIONALLY OUTDATED so that SCA tools
# (trivy fs, pip-audit) will flag known vulnerabilities.
# CWE-1104 (use of unmaintained 3rd-party components),
# CWE-1395 (dependency on vulnerable 3rd-party component),
# OWASP 2025 A03 Software Supply Chain Failures.
#
# DO NOT use these pins in real projects. Treat the SCA output as a
# remediation worklist: bump each to a current, patched release.

Flask==0.12.2          # very old; many CVEs (e.g. debugger/JSON issues)
Werkzeug==0.14.1       # pinned to stay compatible with Flask 0.12.x; old CVEs
Jinja2==2.10           # CVE-class: sandbox escape / ReDoS in old releases
requests==2.19.1       # old HTTP client; CVEs around redirects/cert handling
urllib3==1.24.1        # CVE-2019-11324 / CVE-2019-11236 era — CRLF / cert bugs
PyYAML==3.13           # CVE-2017-18342 — unsafe full_load default
